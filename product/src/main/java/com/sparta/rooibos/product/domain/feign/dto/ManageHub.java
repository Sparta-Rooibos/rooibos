package com.sparta.rooibos.product.domain.feign.dto;

public record ManageHub(String id,
                        String name,
                        String region,
                        String address) {
}
